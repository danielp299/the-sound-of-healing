import React from "react";
import { View, Text } from "react-native";

const Game = ({ navigation }) => {


    return (
        <View style={{flex: 1, justifyContent: "center", flexDirection: "column", alignItems: "center", alignContent: "center" }}>
            <Text>THis is GAME PLAY!</Text>
        </View>
    )
}

export default Game;